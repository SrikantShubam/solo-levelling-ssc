[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ssc-corpus"
version = "0.1.0"
description = "SSC corpus acquisition and integrity tooling."
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
